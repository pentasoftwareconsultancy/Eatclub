import React from 'react';

// The specific menu page screenshots do not have a large hero banner.
// They just have the menu. We will render nothing here or a very simple spacer.
export const Hero: React.FC = () => {
  return null;
};